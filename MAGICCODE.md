# راهنمای MagicCode (بدون دسترسی به فایل‌های اندروید)

این زیپ **کامل** است و پوشهٔ `android` از قبل داخلش هست.

## کارهایی که تو باید بکنی

1. همین فایل زیپ را در MagicCode آپلود کن (همان‌طور که همیشه می‌کنی).
2. اگر گزینهٔ **App Name / نام برنامه** دارد، بنویس: **فرنو یار**
3. اگر گزینهٔ **Package / Application ID** دارد: `com.farno.yar`
4. اگر گزینهٔ **Build type** دارد: **Release** را انتخاب کن (نه Debug).
5. اگر گزینهٔ **Split APK** یا **ABI** یا **معماری** دارد:
   - فقط **arm64-v8a** یا **armeabi-v7a + arm64** را بزن
   - Universal / All architectures را خاموش کن
6. بیلد بگیر و APK را دانلود کن.

## اگر حجم هنوز ~۵۰ مگ شد

MagicCode احتمالاً یک APK برای همهٔ CPUها می‌سازد.
در خروجی بیلد اگر چند فایل دیدی، این را نصب کن:

- `app-arm64-v8a-release.apk`  ← گوشی‌های جدید (پیشنهادی)
- `app-armeabi-v7a-release.apk` ← گوشی‌های قدیمی‌تر

نه فایل `app-release.apk` (universal که بزرگ است).

## نام «فرنو یار»

داخل این پروژه از قبل در Manifest نوشته شده:

```
android:label="فرنو یار"
```

اگر MagicCode موقع بیلد نام را از تنظیمات خودش عوض کرد، در همان پنل نام را به **فرنو یار** تغییر بده.

## آیکون

فایل `assets/icon.png` داخل زیپ است.
اگر MagicCode فیلد آپلود آیکون دارد، همان `icon.png` را آنجا هم آپلود کن.
