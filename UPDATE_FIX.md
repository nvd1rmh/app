# چرا می‌گوید ناسازگار و باید پاک کنم؟

اندروید فقط وقتی APK را «آپدیت» می‌داند که:

1. **applicationId یکسان** باشد → ما همیشه `com.farno.yar` می‌گذاریم
2. **versionCode بالاتر** باشد → هر بیلد عدد `+N` در pubspec بالاتر برود (الان `1.6.0+8`)
3. **با همان کلید امضا (signing key)** ساخته شود

اگر Codemagic هر بار با کلید debug/متفاوت امضا کند، اندروید می‌گوید ناسازگار.

## کار درست در Codemagic

- یک **keystore** ثابت بساز و در Codemagic به‌عنوان environment file/secret نگه دار
- در workflow همیشه همان keystore را برای release استفاده کن
- `applicationId` را عوض نکن
- فقط `version: x.y.z+N` را بالا ببر (عدد بعد از + مهم است)

تا وقتی keystore عوض نشود، نصب روی نسخه قبلی بدون پاک کردن کار می‌کند.
