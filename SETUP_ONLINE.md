# راهنمای راه‌اندازی آنلاین فرنو یار (ایران)

## واقعیت مهم درباره سرور رایگان در ایران

Firebase و خیلی از سرویس‌های خارجی روی **نت ملی** ناپایدار یا فیلترند.
برای اینکه مشتری با اینترنت ایران بدون VPN وصل شود:

### بهترین مسیر عملی
**PocketBase** (یک فایل سبک) روی **هاست ایرانی**:
- لیارا (Liara)
- ابرآروان
- پارس‌پک / ایران‌سرور (VPS ارزان)

کاملاً «رایگان دائمی با ترافیک بالا» معمولاً وجود ندارد؛ ولی PocketBase روی یک VPS کوچک ایرانی ارزان و پایدار است.
بعضی پلتفرم‌ها طرح آزمایشی رایگان دارند.

---

## قدم ۱ — نصب PocketBase روی هاست ایرانی

1. از https://pocketbase.io فایل لینوکس را بگیر
2. روی سرور اجرا کن: `./pocketbase serve --http=0.0.0.0:8090`
3. دامنه/ساب‌دامنه را به آن وصل کن (HTTPS)
4. پنل ادمین را در مرورگر باز کن و ادمین بساز

### کالکشن `users`
- type: Auth
- فیلدها:
  - `name` (text, required)
  - `phone` (text, required, unique)
  - `address` (text)
- Identity: از **username** استفاده می‌کنیم = همان شماره موبایل
- در تنظیمات Auth اجازه ثبت‌نام عمومی (Create rule) را برای کاربران مهمان باز کن:
  - Create rule: خالی یا `true` (فقط برای شروع؛ بعداً سخت‌گیرتر کن)

### کالکشن `products` (فاز فروشگاه)
- name, price, stock, description, image (اختیاری)
- List rule: عمومی خواندن (`true`)

### کالکشن `orders`
- user, items (json), total, status, address, phone, created
- Create فقط برای کاربر لاگین‌شده

---

## قدم ۲ — اتصال اپ

فایل: `lib/services/config.dart`

```dart
static const String apiBaseUrl = 'https://YOUR-DOMAIN';
```

دوباره APK بساز (نسخه را در pubspec بالا ببر تا روی گوشی «آپدیت» بخورد نه نصب جدا).

---

## قدم ۳ — آپدیت بدون نصب دوباره

- `applicationId` همیشه `com.farno.yar` بماند
- هر بیلد: `version: x.y.z+N` را افزایش بده (مثلاً `1.3.0+4` → `1.3.1+5`)
- APK جدید را روی همان گوشی نصب کن → اندروید می‌گوید آپدیت

قیمت و لیست قطعات را از سرور بخوان؛ لازم نیست برای هر تغییر قیمت APK جدید بدهی (مثل دیجی‌کالا).

---

## قدم ۴ — خبر سفارش به تلگرام (بدون دردسر فیلتر برای مشتری)

مشتری فقط به **API خودت** (سرور ایرانی) وصل می‌شود.
سرور تو (که به api.telegram.org دسترسی دارد یا از پروکسی سرور استفاده می‌کند) پیام را به ربات می‌فرستد.

در PocketBase می‌توانی با Hook (JS) بعد از ساخت سفارش:

```
POST https://api.telegram.org/botTOKEN/sendMessage
chat_id=YOUR_CHAT_ID
text=سفارش جدید ...
```

اگر سرور داخل ایران به تلگرام وصل نبود، از یک Worker خارجی خیلی سبک فقط برای `sendMessage` استفاده کن؛ اپ مشتری مستقیم به تلگرام وصل نمی‌شود.

---

## قدم ۵ — Codemagic / بیلد

- Flutter 3.24.5
- APK + `--split-per-abi --tree-shake-icons`
- بعد از گذاشتن `apiBaseUrl` دوباره بیلد بگیر

---

## چک‌لیست

- [ ] PocketBase روی هاست ایرانی بالا است
- [ ] HTTPS دارد
- [ ] کالکشن users آماده است
- [ ] apiBaseUrl در config.dart پر شده
- [ ] versionCode از نسخه قبلی بیشتر است
- [ ] تست ثبت‌نام با دو گوشی / دو شماره
